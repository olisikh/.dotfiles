#!/usr/bin/env bash

set -euo pipefail

dotfiles_root="$1"
system="$2"
script_dir="$3"
skip_gc="$4"
shift 5 # The profile has no post-switch actions.

darwin-rebuild switch --flake "$dotfiles_root#$system" "$@"

if [[ "$skip_gc" != true ]]; then
    "$script_dir/nix-prune-system-generations"
fi
